{% extends 'base.html.twig' %}

{% block title %}{{ 'Reset password'|trans }}{% endblock %}

{% block body %}
   <section>
    <h1>{{ 'Reset password'|trans }}</h1>
    {{ form_start(newPasswordForm) }}
        {{ form_row(newPasswordForm.password) }}
        <button type="submit">{{ 'Send'|trans }}</button>
    {{ form_end(newPasswordForm) }}
    </section>
{% endblock %}
